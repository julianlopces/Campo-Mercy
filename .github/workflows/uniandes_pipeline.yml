name: Mercy Corps - Pipeline

on:
  schedule:
    - cron: "0 */2 * * *" # cada 2 horas (UTC)
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: mercy-alertas
  cancel-in-progress: true

jobs:
  run-alertas:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    env:
      GITHUB_ACTIONS: "true"
      SERVIDOR: ${{ secrets.SERVIDOR }}
      PASSWORD: ${{ secrets.PASSWORD }}
      EMAIL: ${{ secrets.EMAIL }}
      FORMID: ${{ secrets.FORMID }}
      IDALERTAS: ${{ secrets.IDALERTAS }}
      GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
      # Si odkmissing fuera privado, descomenta y crea el secret GITHUB_PAT
      # GITHUB_PAT: ${{ secrets.GITHUB_PAT }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "release"

      - name: Install system dependencies (sf)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libudunits2-dev \
            libgdal-dev \
            libgeos-dev \
            libproj-dev

      - name: Cache R packages
        uses: actions/cache@v4
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ hashFiles('**/*.R') }}
          restore-keys: |
            ${{ runner.os }}-r-

      - name: Install pak
        run: |
          Rscript -e 'if (!requireNamespace("pak", quietly = TRUE)) install.packages("pak", repos = "https://cloud.r-project.org")'

      - name: Install R packages (incl. GitHub odkmissing)
        run: |
          Rscript -e 'pak::pkg_install(c(
            "dplyr", "tidyr", "httr", "jsonlite",
            "googledrive", "googlesheets4", "writexl",
            "haven", "stringr", "labelled", "lubridate",
            "gtsummary", "dotenv", "readxl", "tibble",
            "sf", "purrr", "pacman", "tidyverse",
            "julianlopces/odkmissing"
          ))'

      - name: Run Mercy Corps pipeline
        run: |
          Rscript -e "sessionInfo()"
          Rscript Scripts/Mercy_Corps_Master_script.R